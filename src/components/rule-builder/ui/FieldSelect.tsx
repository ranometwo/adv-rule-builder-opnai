import { Select,SelectContent,SelectItem,SelectTrigger,SelectValue } from '@/components/ui/select'; import { FIELD_CATALOG } from '../data/catalog'; export default function FieldSelect({value,onChange}:{value:string;onChange:(v:string)=>void}){return(<Select value={value} onValueChange={onChange}><SelectTrigger className='w-56'><SelectValue placeholder='Field'/></SelectTrigger><SelectContent>{Object.entries(FIELD_CATALOG).map(([group,items])=>(<div key={group}><div className='px-2 py-1 text-xs text-slate-400'>{group}</div>{(items as any).map((i:any)=>(<SelectItem key={i.field} value={i.field}>{i.label}</SelectItem>))}</div>))}</SelectContent></Select>) }