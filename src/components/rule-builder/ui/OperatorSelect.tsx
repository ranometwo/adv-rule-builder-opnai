import { Select,SelectContent,SelectItem,SelectTrigger,SelectValue } from '@/components/ui/select'; import { fieldMeta } from '../data/catalog'; import { NUMERIC_OPS,SET_OPS,BOOL_OPS,EXISTENCE_OPS } from '../data/constants'; export default function OperatorSelect({field,value,onChange}:{field:string;value:string;onChange:(v:string)=>void}){const meta=fieldMeta(field); const type=(meta?.type as string)||'number'; const opts=[...(type.includes('number')?Array.from(NUMERIC_OPS):[]),...(type.includes('multiselect')||type.includes('string')?Array.from(SET_OPS):[]),...(type.includes('boolean')?Array.from(BOOL_OPS):[]),...Array.from(EXISTENCE_OPS)]; return(<Select value={value} onValueChange={onChange}><SelectTrigger className='w-40'><SelectValue placeholder='Operator'/></SelectTrigger><SelectContent>{opts.map(op=>(<SelectItem key={op as string} value={op as string}>{op as string}</SelectItem>))}</SelectContent></Select>) }